package hhssAdv;

/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */

/**
 *
 * @author slatz8075
 */
public class Player {

    /**
     * @param args the command line arguments
     */
    private int direction = 0;
    private int location = 0;

    public void move() {
        //access the location array at the position we are currently at

        //move through the words in this array until we have found the direction we are facing
        while(location[location]){
            //move to the next word
            location[location].next();
        }
        //now move through this section until we have found either true or false
        if ( == true) {
            //we know that there are no instructions here to follow so do nothing
        } else {
            //there
        }
    }

    public turnLeft() {
    }

    public turnRight() {
    }
}
